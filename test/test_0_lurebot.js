'use strict';

const { expect } = require('chai');

const { Lurebot } = require('../lib/main');
const { DebugAdapter } = require('../lib/adapters/debug');

describe('Lurebot', function () {
  let lurebot = new Lurebot();
});
